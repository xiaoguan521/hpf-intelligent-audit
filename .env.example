# NVIDIA API 配置（推荐）
NVIDIA_API_KEY=nvapi-your-key-here

# 备选 API（可选）
OPENAI_API_KEY=sk-your-openai-key
ANTHROPIC_API_KEY=sk-ant-your-claude-key

# LLM 配置
DEFAULT_LLM_PROVIDER=nvidia
DEFAULT_LLM_MODEL=z-ai/glm4.7
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
OPENAI_BASE_URL=https://api.openai.com/v1
ANTHROPIC_BASE_URL=https://api.anthropic.com
CEREBRAS_BASE_URL=https://api.cerebras.ai/v1

# 备选模型配置
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
CEREBRAS_MODEL=zai-glm-4.7

EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_KEY=sk-你的key

# ========================================
# Embedding 模型配置（向量检索）
# ========================================
# 提供商选择: openai / cerebras / nvidia / deepseek
EMBEDDING_PROVIDER=openai

# 通用模型配置（优先级最高，会覆盖特定提供商的配置）
EMBEDDING_MODEL=text-embedding-3-small

# 各提供商特定配置（可选）
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
CEREBRAS_EMBEDDING_MODEL=text-embedding-3-small
NVIDIA_EMBEDDING_MODEL=NV-Embed-QA
DEEPSEEK_EMBEDDING_MODEL=text-embedding-3-small

# DeepSeek API 配置（如果使用 DeepSeek）
DEEPSEEK_API_KEY=sk-your-deepseek-key
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# 使用说明：
# 1. 复制此文件为 .env
# 2. 填入您的 API Key
# 3. 根据需要调整模型和URL配置
# 4. 运行: pip install python-dotenv

# ========================================
# API 路径前缀配置（可选）
# ========================================
# 为所有 API 和静态页面添加统一路径前缀
# 例如: API_PREFIX=gjjrgznzd
# 访问路径示例: /gjjrgznzd/api/knowledge/list
# 留空则不添加前缀
API_PREFIX=

# ========================================
# Oracle 数据库配置（ETL 源）
# ========================================
ORACLE_USER=your_oracle_user
ORACLE_PASSWORD=your_oracle_password
ORACLE_HOST=localhost
ORACLE_PORT=1521
ORACLE_SCHEMA=YOUR_SCHEMA
ORACLE_SERVICE=ORCL

# ============================================================
# Oracle 驱动模式配置（可选）
# ============================================================
# 强制使用特定模式（不推荐，建议让系统自动检测）
# ORACLE_FORCE_MODE=thin      # 强制 Thin 模式（仅 Oracle 12.1+）
# ORACLE_FORCE_MODE=thick     # 强制 Thick 模式（所有版本）

# Thick 模式专用：Oracle Instant Client 路径
# 如果不设置，系统会自动在以下位置查找：
# - etl/instantclient_*
# - /opt/oracle/instantclient_*  
# - C:\instantclient_*
# ORACLE_CLIENT_DIR=/path/to/instantclient_23_0

# 版本兼容性说明：
# - Oracle 12.1+: 自动使用 Thin 模式（无需 Instant Client）
# - Oracle 11g:   自动使用 Thick 模式（需要 Instant Client）

# ========================================
# DuckDB 数据仓库配置（ETL 目标）
# ========================================
DUCKDB_PATH=./data/warehouse.duckdb
DUCKDB_DATASET=ods  # 默认数据集名称（对应 DuckDB 中的 schema）
